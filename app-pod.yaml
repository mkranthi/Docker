apiVersion: v1
kind: Pod
metadata:
  name: ngnix-pod
spec:
  containers:
    - name: deploy-pod
      image: devcompute.jfrog.io/itsoli-docker-develop-local/demo2-image
      ports:
        - containerPort: 80
